services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: patio-web:local
    ports:
      - "${WEB_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
    restart: unless-stopped
